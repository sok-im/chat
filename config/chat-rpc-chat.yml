rpc:
  # The IP address where this RPC service registers itself; if left blank, it defaults to the internal network IP
  registerIP: ''
  # IP address that the RPC service listens on; setting to 0.0.0.0 listens on both internal and external IPs. If left blank, it automatically uses the internal network IP
  listenIP: 0.0.0.0
  # List of ports that the RPC service listens on; configuring multiple ports will launch multiple instances.
  ports: [ 30300 ]

verifyCode:
  validTime: 300
  validCount: 5
  uintTime: 86400
  maxCount: 10
  verifyMinuteCount: 0
  # When verify-code failures on the current code reach this count, needVerifyCaptcha=true is returned (0 = disabled).
  captchaFailCount: 3
  superCode: "666666"
  len: 6
  phone:
    use: "" # "ali" | "twilio" | empty (SMS disabled; superCode only if mail also off)
    ali:
      endpoint: ""
      accessKeyId: ""
      accessKeySecret: ""
      signName: ""
      verificationCodeTemplateCode: ""
    twilio:
      accountSid: ""
      authToken: ""
      # Twilio-owned number in E.164, e.g. "+12025551234", or Messaging Service SID "MG..."
      from: ""
      body: "Your verification code is: %s"
      bodyTemplates:
        default: "Your verification code is: %s"
        en: "Your verification code is: %s"
        zh-cn: "您的验证码是：%s"
      defaultLanguage: "en"
  mail:
    enable: false
    title: ""
    senderMail: ""
    senderAuthorizationCode: ""
    smtpAddr: ""
    smtpPort:

liveKit:
  url: "ws://127.0.0.1:7880" # LIVEKIT_URL, LiveKit server address and port
  key: ""
  secret: ""

allowRegister: true
maxAccountsPerPhone: 2
